import React from 'react';
import '../scss/fallback.scss';

const Fallback = () => {
    return (
        <div className='fallback'>
            Loading...
        </div>
    );
}

export default Fallback;
